{
  "name": "blockchain-a1-erc20",
  "version": "1.0.0",
  "description": "ERC-20 Token Deployment to Sepolia Testnet — Assignment 1 (S4112914)",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy-and-interact.js --network sepolia",
    "deploy:local": "npx hardhat run scripts/deploy-and-interact.js --network hardhat"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.0"
  }
}
